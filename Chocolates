#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int x, y, z;
        scanf("%d %d %d", &x, &y, &z);
        int total_money=x*5+y*10;
        int chocolates=total_money/z;
        printf("%d\n",chocolates);
    }

}
